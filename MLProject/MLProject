name: xgboost-classification-project

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      experiment_name: {type: str, default: "Experiment_XGBoost_AutoLog"}
    command: "python modelling.py --experiment-name {experiment_name}"
